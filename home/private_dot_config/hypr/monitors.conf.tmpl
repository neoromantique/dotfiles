# Monitor Configuration
# Device: {{ .deviceProfile }} ({{ .hostname }})

# Primary monitor only (widescreen)
monitor = {{ .primaryMonitor }},{{ .primaryResolution }},auto,1

# External displays (when connected)
monitor = ,preferred,auto,1
